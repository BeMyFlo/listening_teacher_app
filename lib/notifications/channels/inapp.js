// Kênh in-app (chuông). Bản ghi Notification tồn tại là đã "gửi" — chuông
// đọc trực tiếp qua GET /api/notifications. Không cần làm gì thêm ở đây.
module.exports.deliver = async function deliver(/* notif */) {};
